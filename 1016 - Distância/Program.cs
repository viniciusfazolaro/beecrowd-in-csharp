using System; 
using System.Globalization;

class URI {

    static void Main(string[] args) { 

        int distance = int.Parse(Console.ReadLine()), time;

        time = distance * 2;

        Console.WriteLine(time + " minutos");        
    }
}